#pragma once

// This file is just an alias for Eule's Vector files.
// It would be very tedios having to include "../Eule/Vector3.h" every time you want to use vectors
// within the game engine. Much more convenient to just include "Vector.h".
// Also, when using the game engine, one should ideally not have to interact with the maths engine at all.

#include "../Eule/Vector2.h"
#include "../Eule/Vector3.h"
#include "../Eule/Vector4.h"
